# clawuse2

OpenClaw single-repo vault (non-sensitive artifacts only).

- Do NOT commit secrets (tokens, passwords, private keys).
- Use `uploads/` for exported files.
- Use `skills/` for skill drafts/snapshots.
